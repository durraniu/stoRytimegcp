drawing_instructions <- "This scene should be illustrated in a storybook style with soft, pastel colors, whimsical and child-friendly illustrations with gentle lines. The image should evoke warmth and wonder, resembling hand-drawn artwork with simplicity and charm, like classic fairy tales."

# @importFrom RPostgres Postgres
# .onLoad <- function(libname, pkgname) {
#   # This ensures that RPostgres is loaded
#   invisible(RPostgres::Postgres())
# }

# data_storage <- shiny.telemetry::DataStoragePostgreSQL$new(
#   username = Sys.getenv("POSTGRES_USER"),
#   password = Sys.getenv("POSTGRES_PASS"),
#   hostname = Sys.getenv("POSTGRES_HOSTNAME"),
#   port = 6543,
#   dbname = "postgres",
#   driver = "RPostgres"
# )




# telemetry <- shiny.telemetry::Telemetry$new(
#   app_name = "storytimegcp",
#   data_storage = data_storage
# )
